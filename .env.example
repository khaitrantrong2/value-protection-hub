# Google Apps Script Web App URL that serves Links / Categories / Config as JSON.
# Leave empty during local development to use the bundled mock data.
# NEVER commit a real deployment URL here if the Apps Script exposes non-public data
# without its own access check — see apps-script/README.md.
VITE_LINKS_API_URL=
